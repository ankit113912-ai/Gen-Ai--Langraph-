# simple self-Hosted  Deployment (suru karne ke liye )

"""
# LangGraph CLI install karo
pip install langgraph-cli

# Apne project mein ek config file banao: langgraph.json

{
  "dependencies": ["."],
  "graphs": {
    "my_agent": "./my_agent.py:app"
  },
  "env": ".env"
}

# Local server chalao (testing ke liye)
langgraph dev

langgraph dev
# Fir browser mein LangGraph Studio khol ke apne local server se connect karo

"""
